<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="Call A Mechanic"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallAMechanic"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.client.ClientDashboardActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.client.BookAppointmentActivity" />
        <activity android:name=".ui.admin.UserRegistryActivity" />
        <activity android:name=".ui.admin.CreateMechanicActivity" />
        <activity android:name=".ui.mechanic.MechanicDashboardActivity" />
        <activity android:name=".ui.appointment.AppointmentDetailsActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
